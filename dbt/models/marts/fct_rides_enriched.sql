select
  ride_id,
  distance,
  cab_type,
  ride_time_stamp,
  ride_timestamp,
  ride_hour,
  destination,
  source,
  price,
  surge_multiplier,
  ride_name,
  weather_id,
  temp,
  clouds,
  pressure,
  rain,
  humidity,
  wind,
  location,
  weather_time_stamp,
  weather_timestamp,
  weather_hour
from {{ ref('int_rides_weather_enriched') }}
